# Microsoft Store assets

These assets are generated from the verified public PWA by the repository's Playwright workflow.

Required current run: capture 1440×900 PNG screenshots for home, CSV loaded preview, and cleaned result, then persist `store_asset_state.json` only after browser capture succeeds.
