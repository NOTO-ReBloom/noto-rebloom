name: BOOTH paid recovery verifier (retired)

# 2026-08-22: the 010-023 recovery batch reached strict buyer-visible 14/14.
# Automatic cron/push triggers are intentionally retired so completed recovery
# work cannot keep generating unnecessary GitHub Actions runs or failure emails.
# Keep manual dispatch only for audit/debug use.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm retired recovery verifier
        run: |
          echo "BOOTH recovery verifier is retired: 010-023 were strictly verified 14/14 on 2026-08-22."
          echo "Future BOOTH batches must use the current direct buyer-visible verifier, not this dated recovery workflow."
