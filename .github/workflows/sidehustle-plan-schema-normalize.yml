name: Normalize current sidehustle plan schema

on:
  push:
    paths:
      - '.github/workflows/sidehustle-plan-schema-normalize.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  normalize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Normalize free NOTE compatibility fields
        run: |
          python - <<'PY'
          from pathlib import Path
          import json
          p=Path('sidehustle-autopublish/builders/daily_plan_current.json')
          d=json.loads(p.read_text('utf-8'))
          paid=d.get('paid',[])
          palette=['#315E81','#6B4E71','#2E6F5E','#7A5A32']
          changed=False
          for i,x in enumerate(d.get('free',[])):
              defaults={
                  'paired': x.get('pairedPaid') or (paid[i]['id'] if i < len(paid) else ''),
                  'coverLabel': '無料チェック',
                  'coverCopy': x.get('title','').split('｜')[0][:24],
                  'accent': palette[i % len(palette)],
              }
              for k,v in defaults.items():
                  if not x.get(k):
                      x[k]=v
                      changed=True
          if changed:
              p.write_text(json.dumps(d,ensure_ascii=False,indent=2)+'\n','utf-8')
              print('NORMALIZED_CURRENT_PLAN')
          else:
              print('PLAN_ALREADY_NORMALIZED')
          PY
      - name: Commit normalization
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add sidehustle-autopublish/builders/daily_plan_current.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'Normalize current sidehustle plan schema'
          git pull --rebase origin main
          git push

# canonical schema compatibility repair
